import { makeStyles } from '@mui/styles';

const useStyles = makeStyles({
  container: {
    display: 'flex',
    paddingTop: 60,
  },
  flatContainer: {
    position: 'fixed',
    left: '45%',
  },
  title: {
    marginTop: 133,
    marginBottom: 50,
  },
});

export default useStyles;
